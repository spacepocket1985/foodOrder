export type DummyMealsType = {
  id: string;
  name: string;
  description?: string;
  price: number;
};

export type CartItemType = {
  id: string;
  name: string;
  price: number;
  amount: number;
};
